# Dev/test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0
httpx>=0.27.0
coverage>=7.4.0
# Provides the pytest.mark.anyio plugin used by the async pipeline tests.
# Available transitively via starlette/httpx/openai, but declared so a
# slimmer CI image doesn't silently skip those tests.
anyio>=4.0.0
